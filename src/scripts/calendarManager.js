/**
 * Adding, removing and sharing calendars
 * Calendar table key- Calendar ID and user ID
 * Shared by creating new entry with same calendar, shared user
 * Permission checks- can only share/delete if require permissions are met
 */